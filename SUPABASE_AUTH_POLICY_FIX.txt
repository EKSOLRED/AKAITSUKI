1. Если после регистрации вход не пускает:
   - теперь сайт покажет отдельное сообщение:
     "Подтверди почту через письмо и попробуй войти снова"

2. Если после подтверждения входа видишь:
   - infinite recursion detected in policy for relation "profiles"

   Значит старая policy на profiles ссылалась на public.profiles прямо из самой себя.

3. Исправление:
   - выполни новый supabase/005_profiles_rls.sql
   - потом выполни новый supabase/008_content_rls.sql

4. Эти файлы переводят проверку ролей в SECURITY DEFINER функции:
   - public.is_owner(uuid)
   - public.is_admin_or_owner(uuid)

   Из-за этого RLS больше не уходит в рекурсию.
