Что изменено:
- logout сразу очищает текущего пользователя и шлёт событие обновления UI;
- app.js подписан на изменения auth-состояния и сразу перерисовывает шапку;
- после login/register профиль заново синхронизируется из profiles;
- если профиля нет, он создаётся автоматически.

Если owner всё ещё не виден:
1. выполни SUPABASE_DEBUG_ROLE.sql
2. убедись, что email в public.profiles совпадает с email аккаунта
3. выйди и зайди снова
